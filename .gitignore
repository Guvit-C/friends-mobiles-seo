# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Results file
results.tsv

# SEO agent run log (uploaded as GitHub Actions artifact, not committed)
seo_run.log

# Credentials — never commit
.env
.env.local
credentials.json

# Strategy backup (created before each overwrite, not needed in git)
seo_strategy.py.bak
